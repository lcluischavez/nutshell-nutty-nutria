const friendsComponent = (friends, users) => {
    return `
        <section class="friend">
            <div>
                <h3> ${friends.userId} </h3>
            </div>
        </section>
    `
}

export default friendsComponent

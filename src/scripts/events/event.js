const eventComponent = (events, users) => {
    return `
        <section class="event">
            <div>
                <h3> ${events.name} </h3>
            </div>
        </section>
    `
}

export default eventComponent
